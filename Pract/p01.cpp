#include <iostream>
#include <cmath>
int main()
{
int n;
std::cin >> n;
    int sum =0;
for (int i = 1; i <=n; i++ ) {
sum =+i;
    double y=sin(n-i+1);
std::cout<<sin(sum)<<std::endl;
for (int j = 1; j <= n; j++ )
    sum =+n+1;
    double b = cos(1+n);
    std::cout<<cos(b)<<std::endl;
}
 double c=cos(n)/sin(n);
    std::cout<<c<<std::endl;
    return 0;



}